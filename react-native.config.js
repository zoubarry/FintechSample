module.exports = {
  project: {
    android: {
      // Tell the CLI that the android project is in the 'android' folder
      sourceDir: './android', 
      // Point specifically to the app module
      appName: 'app',
    },
  },
};
